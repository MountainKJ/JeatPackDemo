<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kj.x.jd">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LockScreenActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:taskAffinity="kj.x.jd.test.lockscreen"/>
        <activity android:name=".DataBindingTest"/>
        <activity android:name=".jeatpack.JeatPackDemoActivity"/>
        <activity android:name=".jeatpack.JeatPackActivity_AndroidKTX"/>
        <activity android:name=".main.HomePageActivity"/>
    </application>

</manifest>